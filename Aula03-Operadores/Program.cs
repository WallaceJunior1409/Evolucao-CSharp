using System;

namespace Aula03
{
    class Program
    {
        static void Main(string[] args)
        {
            int assignment = 10; // = -> atribui um valor a uma variavel
            int some = 10 + 5; // + -> soma de valores

            //variavel 'some' revebera ela mesma + 1
            //some = some + 1; some += 1; some++;
            //some = some + 2; some += 2; + - / *

            bool boolean = 10 > 5; // > < >= <=

            // & = AND / E -> retorna verdadeiro se todas as operações der verdadeiro
            // | = OR / OU -> retorna verdadeiro se uma das operações der verdadeiro
            bool logic = (5 > 3) | (7 < 3);


        }
    }
}
